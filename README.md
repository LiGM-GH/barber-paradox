# Barber paradox

The project has three files:
`main.rs` with the most practical implementation,
`comptime.rs` with the most complex implementation,
and `mutually_exclusive_traits.rs` with the most simple implementation.
